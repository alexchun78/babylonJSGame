{   
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "program": "${workspaceFolder}/app/src/app.ts",
      //"preLaunchTask": "tsc: build - tsconfig.json",
      "outFiles": ["${workspaceFolder}/app/dist/**/*.js"]
    }
  ]
}